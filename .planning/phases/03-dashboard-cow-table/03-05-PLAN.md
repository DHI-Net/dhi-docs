---
phase: 03-dashboard-cow-table
plan: 05
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03, 03-04]
files_modified:
  - frontend/src/views/OverviewView.vue
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - FILT-01
  - FILT-02
  - FILT-03
  - COW-01
  - COW-02
  - COW-03
  - COW-04

must_haves:
  truths:
    - "OverviewView integrates overviewFilter store for farm filtering"
    - "Clicking a farm card in OverviewView switches role to farm and navigates to /farm/dashboard"
    - "All 6 routes render correctly with sidebar navigation"
    - "All single-farm views share filter state and react to farm/month changes"
    - "TopBar shows farm/month selectors on /farm/* routes and page title on /overview* routes"
    - "Role switcher in sidebar navigates to correct default route"
  artifacts:
    - path: "frontend/src/views/OverviewView.vue"
      provides: "OverviewView wired with overviewFilter store"
  key_links:
    - from: "frontend/src/views/OverviewView.vue"
      to: "frontend/src/stores/overviewFilter.js"
      via: "reads selectedFarms for farm card filtering"
      pattern: "useOverviewFilterStore"
---

<objective>
Wire OverviewView with the overviewFilter store for farm filtering, then run a full visual verification of all 6 routes, sidebar navigation, role switching, and filter interactions.

Purpose: Ensure all plans integrate correctly. OverviewView needs to respect selectedFarms from overviewFilter. This plan also serves as the final integration checkpoint.

Output: Wired OverviewView + visual verification that the complete Phase 3 redesign works end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-cow-table/03-CONTEXT.md
@.planning/phases/03-dashboard-cow-table/03-UI-SPEC.md
@.planning/phases/03-dashboard-cow-table/03-01-SUMMARY.md
@.planning/phases/03-dashboard-cow-table/03-02-SUMMARY.md
@.planning/phases/03-dashboard-cow-table/03-03-SUMMARY.md
@.planning/phases/03-dashboard-cow-table/03-04-SUMMARY.md

<interfaces>
<!-- From Plan 02: overviewFilter store -->
From frontend/src/stores/overviewFilter.js:
  useOverviewFilterStore() -> {
    selectedFarms: Set<string>,
    timeRange, savedPresets,
    toggleFarm, savePreset, applyPreset, deletePreset, resetAll
  }

<!-- OverviewView current state (from Plan 01) -->
OverviewView.vue already:
- Renders farm cards with KPIs and mini trend charts
- Has comparison table
- Has goToDashboard() that calls farmStore.setRole('farm') and router.push('/farm/dashboard')
- Reads from window.__DHI_DATA__

What needs adding:
- Import useOverviewFilterStore
- Filter farmSummaries computed by selectedFarms (if not empty Set, only show selected farms)
- Add farm checkbox filter panel (same as TrendComparisonView)
- Add preset save/load dropdown
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire OverviewView with overviewFilter store for farm filtering</name>
  <files>frontend/src/views/OverviewView.vue</files>
  <read_first>
    - frontend/src/views/OverviewView.vue
    - frontend/src/stores/overviewFilter.js
    - frontend/src/views/TrendComparisonView.vue
  </read_first>
  <action>
Modify `frontend/src/views/OverviewView.vue` to integrate overviewFilter store:

1. **Add imports:**
```javascript
import { useOverviewFilterStore } from '../stores/overviewFilter.js'

const overviewFilter = useOverviewFilterStore()
const { selectedFarms } = storeToRefs(overviewFilter)
```

2. **Filter farmSummaries by selectedFarms:**
In the existing `farmSummaries` computed, add filtering at the start:
```javascript
const farmSummaries = computed(() => {
  const staticData = window.__DHI_DATA__ || {}
  let farmEntries = Object.entries(staticData)

  // Filter by selectedFarms from overviewFilter store
  if (selectedFarms.value.size > 0) {
    farmEntries = farmEntries.filter(([name]) => selectedFarms.value.has(name))
  }

  return farmEntries.map(([farmName, farmData]) => {
    // ... rest of existing logic unchanged
  })
})
```

3. **Add filter panel above the farm cards** (similar to TrendComparisonView):
Insert after the header div and before the farm cards grid:
```html
<!-- Farm filter + preset panel -->
<div class="bg-white rounded-xl shadow-sm border border-gray-100 p-4">
  <div class="flex flex-wrap items-center gap-4">
    <!-- Farm checkboxes -->
    <div class="flex items-center gap-1 text-xs text-gray-500 mr-2">篩選農場：</div>
    <div class="flex flex-wrap gap-3">
      <label v-for="farm in allFarmNames" :key="farm"
        class="flex items-center gap-1.5 text-sm cursor-pointer">
        <input type="checkbox"
          :checked="selectedFarms.size === 0 || selectedFarms.has(farm)"
          @change="overviewFilter.toggleFarm(farm)"
          class="rounded border-gray-300 text-brand-600" />
        {{ farm }}
      </label>
    </div>

    <!-- Preset controls -->
    <div class="ml-auto flex items-center gap-2">
      <select v-if="overviewFilter.savedPresets.length > 0"
        @change="onPresetSelect($event.target.value)"
        class="rounded border border-gray-300 px-2 py-1 text-xs">
        <option value="">常用篩選...</option>
        <option v-for="p in overviewFilter.savedPresets" :key="p.name" :value="p.name">
          {{ p.name }}
        </option>
      </select>
      <button @click="promptSavePreset"
        class="text-xs text-brand-600 hover:text-brand-700 whitespace-nowrap">
        儲存目前篩選
      </button>
    </div>
  </div>
</div>
```

4. **Add helper variables and functions:**
```javascript
const allFarmNames = Object.keys(window.__DHI_DATA__ || {})

function promptSavePreset() {
  const name = window.prompt('請輸入篩選名稱：')
  if (name && name.trim()) {
    overviewFilter.savePreset(name.trim())
  }
}

function onPresetSelect(name) {
  if (name) overviewFilter.applyPreset(name)
}
```

5. **Ensure goToDashboard still works correctly** (should already be updated in Plan 01):
```javascript
function goToDashboard(farmName) {
  farmStore.setFarm(farmName)
  farmStore.setRole('farm')
  router.push('/farm/dashboard')
}
```

Key: Do NOT rewrite the entire OverviewView. Only add the overviewFilter imports, filtering logic, and filter panel UI. Keep all existing farmSummaries computation, farm cards, comparison table, and mini chart logic intact.
  </action>
  <verify>
    <automated>cd /home/elier/Claude/DHI_Net/frontend && npx vite build 2>&1 | tail -3 && grep -c "useOverviewFilterStore" src/views/OverviewView.vue && grep -c "selectedFarms" src/views/OverviewView.vue && grep -c "toggleFarm" src/views/OverviewView.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep "useOverviewFilterStore" frontend/src/views/OverviewView.vue` matches
    - `grep "selectedFarms" frontend/src/views/OverviewView.vue` matches
    - `grep "toggleFarm" frontend/src/views/OverviewView.vue` matches (farm checkbox)
    - `grep "savedPresets" frontend/src/views/OverviewView.vue` matches (preset dropdown)
    - `grep "儲存目前篩選" frontend/src/views/OverviewView.vue` matches (save button)
    - `grep "setRole" frontend/src/views/OverviewView.vue` matches (role switch on card click)
    - `grep "/farm/dashboard" frontend/src/views/OverviewView.vue` matches (updated route)
    - `cd frontend && npx vite build` exits 0
  </acceptance_criteria>
  <done>OverviewView filters farm cards by selectedFarms from overviewFilter store. Farm checkboxes and preset save/load UI integrated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 3 redesign</name>
  <what-built>Complete Phase 3 redesign: sidebar navigation, role switching, 6 routes (overview, trends, dashboard, comparison, cows, alerts), overviewFilter with presets, TopBar conditional display.</what-built>
  <action>Human verifies the assembled Phase 3 redesign visually in a browser. No code changes needed.</action>
  <how-to-verify>
    1. Start the backend: `cd /home/elier/Claude/DHI_Net && python -m uvicorn backend.main:app --reload --port 8000`
    2. Start the frontend: `cd /home/elier/Claude/DHI_Net/frontend && npm run dev`
    3. Open http://localhost:5173 in browser

    **Sidebar + Shell (D-01):**
    4. Verify sidebar is visible on the left, 256px wide, with "DHI Net" logo
    5. Verify main content area is offset to the right of the sidebar

    **Role switching (D-02):**
    6. Verify role switcher at bottom of sidebar shows "顧問模式" selected
    7. Verify sidebar shows 2 items: "多牧場總覽" and "趨勢比較"
    8. Switch to "牧場模式" -- verify sidebar shows 4 items: "儀表板", "比較圖", "牛群資料表", "警示"
    9. Verify navigating to /farm/dashboard automatically

    **TopBar conditional (D-01):**
    10. On /farm/dashboard: verify TopBar shows farm/month selectors
    11. Switch back to "顧問模式" and navigate to /overview
    12. On /overview: verify TopBar shows "多牧場總覽" title text (no farm/month selectors)

    **Overview page (D-04):**
    13. Verify farm cards display with KPIs and mini trend charts
    14. Verify farm checkbox filter panel -- uncheck a farm, verify card disappears
    15. Click "儲存目前篩選" -- enter a name -- verify preset appears in dropdown
    16. Click a farm card -- verify switches to farm role and navigates to /farm/dashboard

    **Trend Comparison (D-08):**
    17. Navigate to "趨勢比較" via sidebar
    18. Verify 3 stacked ECharts line charts (平均乳量, 泌乳頭數, 平均體細胞數)
    19. Verify each chart has one line per farm with different colors
    20. Use time range pills (3/6/12) -- verify charts update
    21. Use farm checkboxes -- verify toggling farms shows/hides their lines

    **Dashboard (existing, rewired):**
    22. Switch to farm role, select a farm in TopBar
    23. Verify KPI cards, FilterBar, TrendChart, ParityChart, SccDistChart, CowTable all render
    24. Click "頭產" in filter -- verify ParityChart does NOT change (isolation)

    **Comparison view (D-03):**
    25. Navigate to "比較圖" via sidebar
    26. Verify grouped bar chart (本月 green, 上月 gray) and comparison table
    27. Verify delta values show correct polarity coloring

    **Cow Table view (D-03):**
    28. Navigate to "牛群資料表" via sidebar
    29. Verify FilterBar + CowTable render
    30. Verify sort, search, pagination, alert badges all work

    **Alerts view (D-09):**
    31. Navigate to "警示" via sidebar
    32. Verify cows grouped by alert code with colored badges
    33. Verify each group shows relevant metric column
    34. Verify (B) SCC group appears first (severity ordering)

    **Navigation consistency:**
    35. Click through all 6 sidebar items -- verify each loads correctly
    36. Verify active sidebar item has brand-colored left border
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint:human-verify -- manual visual verification required"</automated>
  </verify>
  <done>User visually confirms all 6 routes work, sidebar navigation functions correctly, role switching changes nav items, TopBar is conditional, filters sync across views, and all existing functionality (KPI, charts, cow table) is preserved.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| overviewFilter -> OverviewView | Filter state controls which farm data is displayed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | OverviewView.vue | mitigate | Farm filtering uses Set.has() check against staticData keys. No injection vector. All output via Vue auto-escaping. |
| T-03-11 | Information Disclosure | Role switching | accept | Client-side role switching is demo-only. No access control enforcement in Phase 3. Backend auth deferred to Phase 1. |
</threat_model>

<verification>
1. `cd frontend && npx vite build` exits 0
2. `cd frontend && npx vitest run` exits 0
3. All 6 routes render with correct content
4. Sidebar navigation highlights active item
5. Role switching changes sidebar items and navigates to correct default
6. TopBar is conditional: selectors on /farm/*, title on /overview*
7. OverviewView filters farms by overviewFilter.selectedFarms
</verification>

<success_criteria>
- All 6 views render correctly at their routes
- Sidebar navigation with role switching works end-to-end
- TopBar conditionally shows farm/month selectors vs page title
- OverviewView respects selectedFarms filter and supports preset save/load
- All existing Phase 3 functionality preserved (KPI, charts, table, filters)
- Visual checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-cow-table/03-05-SUMMARY.md`
</output>
